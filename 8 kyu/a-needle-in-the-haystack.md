# a-needle-in-the-haystack
https://www.codewars.com/kata/a-needle-in-the-haystack/


```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
return("found the needle at position " + haystack.indexOf("needle"))
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+ haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}


```

```
function findNeedle(haystack) {
 return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' +haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
return 'found the needle at position ' + haystack.indexOf('needle');
  }

```

```
function findNeedle(haystack) {
    return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
};
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}

```

```
function findNeedle(haystack) {
  return "found the needle at position "+ haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+ haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
    return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
   return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
    return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle")


}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
 return ('found the needle at position ' + haystack.indexOf('needle'));
}
```

```
function findNeedle(haystack) {
  // Your code here:
  return "found the needle at position " + haystack.indexOf("needle");
}

```

```
function findNeedle(haystack) {
    return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position "+haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
    return "found the needle at position " + haystack.indexOf("needle");
 }
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return ("found the needle at position "+ haystack.indexOf("needle"));
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " +  haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
return "found the needle at position "+ haystack.indexOf("needle")
  // your code here
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
 return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
    return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here

  return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
    return 'found the needle at position ' + haystack.indexOf('needle');
  }
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
return 'found the needle at position ' + haystack.indexOf('needle');

 }
```

```
function findNeedle(haystack) {
    return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
 return 'found the needle at position '+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return ("found the needle at position "+haystack.indexOf("needle"))
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle");

}
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+ haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return ('found the needle at position '+haystack.indexOf('needle'));
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position '+ haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " +haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
   return ("found the needle at position " + haystack.indexOf("needle"));
};


```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle (haystack) {
  return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {

  return 'found the needle at position ' + haystack.indexOf('needle')

}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {


  return "found the needle at position " + haystack.indexOf('needle');

};
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return ("found the needle at position " + haystack.indexOf("needle"));
}
```

```
function findNeedle(haystack) {
  return ('found the needle at position ' + haystack.indexOf('needle'))
  // your code here
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle');
}

```

```
function findNeedle(haystack) {
 return  'found the needle at position ' + haystack.indexOf("needle")


}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle (haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack){
  return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack)
{
  return "found the needle at position "+haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  // assuming that haystack does indeed contain a 'needle'
  return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+ haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack)
{
  return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
return "found the needle at position " + haystack.indexOf("needle");
  // your code here
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+ haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
 return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
return "found the needle at position " + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
 function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle");
}

```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+ haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return('found the needle at position ' + haystack.indexOf('needle'));
};
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {

  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle (haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here



  return "found the needle at position " + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
 return "found the needle at position "+haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
 return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return  'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle");

}
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' +  haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle (haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + (haystack).indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
    return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
 return ('found the needle at position ' + haystack.indexOf('needle'))
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " +haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
 return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {

  return "found the needle at position " + haystack.indexOf("needle");

}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
   return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle')};
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf("needle");

}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}

```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {

    return "found the needle at position " + (haystack.indexOf("needle"));

}
```

```
function findNeedle(haystack) {
   return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {return "found the needle at position " + haystack.indexOf("needle")}
```

```
function findNeedle(haystack) {
  // your code here
 return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {

  return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf( 'needle' );
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle");

}
```

```
function findNeedle(haystack) {
  return ("found the needle at position " + haystack.indexOf("needle"));
}
```

```
function findNeedle(haystack) {
  // your code here
  // var index = haystack.indexOf("needle");

    return "found the needle at position "+haystack.indexOf("needle");;
}
```

```
function findNeedle(haystack) {
return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code her
  return "found the needle at position "+haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+ haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}

```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {

return "found the needle at position " +  haystack.indexOf("needle");


  // your code here
}
```

```
function findNeedle(haystack) {
 return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+ haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + (haystack.indexOf("needle"));
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position '+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
return "found the needle at position " +  haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf("needle")

}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
 return ("found the needle at position " + haystack.indexOf("needle"));
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
return "found the needle at position " + haystack.indexOf('needle');
// your code here
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
   return "found the needle at position " + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
 return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return ("found the needle at position " + haystack.indexOf("needle"));
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
return ('found the needle at position ' + haystack.indexOf('needle'));
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle");
}





```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' +haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf('needle');

}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " +haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+ haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position '+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
 return "found the needle at position "+haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+ haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
return "found the needle at position " + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {

  return 'found the needle at position ' + haystack.indexOf('needle');
  // your code here
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
    return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
return 'found the needle at position '+haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return ("found the needle at position " + haystack.indexOf("needle"))
}
```

```
function findNeedle(haystack) {
return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return("found the needle at position " + haystack.indexOf("needle"));
}
```

```
function findNeedle(haystack) {
    return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
   return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle");
}

```

```
function findNeedle(haystack) {
  return ("found the needle at position " + haystack.indexOf("needle"));
}

```

```
function findNeedle(haystack) {
   return "found the needle at position " + haystack.indexOf("needle");
      }
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return ('found the needle at position '+ haystack.indexOf('needle'));
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
 return'found the needle at position ' +  haystack.indexOf('needle');
 }
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
 return "found the needle at position " + haystack.indexOf("needle");
  };
```

```
function findNeedle(haystack) {
  return "found the needle at position " +
    haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position "+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
    return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return ("found the needle at position " + haystack.indexOf("needle"));
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+haystack.indexOf('needle');
  ;
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " +  haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
      return "found the needle at position " + haystack.indexOf("needle");
    }
```

```
function findNeedle(haystack) {
 return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {

      return "found the needle at position " + haystack.indexOf("needle");

}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + (haystack.indexOf('needle'));
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle");
}


```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position '+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+ haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
return("found the needle at position " + haystack.indexOf('needle'));
}
```

```
function findNeedle(haystack) {
return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle");
  }
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
    return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack)
{
    return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return('found the needle at position '+haystack.indexOf('needle'));
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf("needle");
}

```

```
function findNeedle(haystack) {
    return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position "+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return  "found the needle at position " + haystack.indexOf("needle");

}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {return 'found the needle at position ' + haystack.indexOf("needle");}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
    return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
return "found the needle at position " + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+ haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
    return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {

     return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
 return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {

  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle");
};
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf("needle");
}

```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here

return "found the needle at position " + haystack.indexOf('needle');

}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return ('found the needle at position '+ haystack.indexOf('needle'));
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}

```

```
function findNeedle(haystack) {
  return 'found the needle at position '+haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
return ("found the needle at position " + haystack.indexOf("needle"));
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
    return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
return "found the needle at position " + haystack.indexOf("needle");

}
```

```

function findNeedle(haystack) {
 return "found the needle at position " + haystack.indexOf("needle");
}

```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
return "found the needle at position " + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here

  return 'found the needle at position ' +  haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+(haystack.indexOf("needle"));
  // your code here
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + (haystack.indexOf('needle'));
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle");

}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {

  return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
return "found the needle at position " + haystack.indexOf("needle");  // your code here
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' +
  haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle");
  }
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle');;
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return ('found the needle at position ' + haystack.indexOf("needle"));
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
      return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
return ("found the needle at position " + haystack.indexOf('needle'));
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack){
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
 return "found the needle at position " + haystack.indexOf("needle"); // your code here
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
    return "found the needle at position " + haystack.indexOf("needle")

}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack){
    return 'found the needle at position ' + haystack.indexOf('needle');
  }





```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here

  return 'found the needle at position ' + haystack.indexOf("needle");

}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
 return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+haystack.indexOf("needle");

  // your code here
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+ haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  // your code here

  return ("found the needle at position " + haystack.indexOf("needle"));
}
```

```
function findNeedle(haystack) {
    return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {

  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return("found the needle at position " + haystack.indexOf('needle'));
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  // your code here
  return ("found the needle at position " + haystack.indexOf("needle"));
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {

   return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {return 'found the needle at position ' + haystack.indexOf('needle')}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
   return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
 return "found the needle at position "+ haystack.indexOf("needle");

}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
 return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here

  return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
 return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
 return 'found the needle at position ' +haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
    return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+ haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
 return "found the needle at position " + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
    return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
return "found the needle at position "+ haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
    return "found the needle at position "+haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {

  return "found the needle at position " + haystack.indexOf("needle");

}
```

```
function findNeedle(haystack) {
  // your code here
      return 'found the needle at position ' + haystack.indexOf('needle');

}
```

```
function findNeedle(haystack) {
   return "found the needle at position " + haystack.indexOf('needle');
}


```

```
function findNeedle(haystack) {
  return "found the needle at position " +haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
return "found the needle at position "+ haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');

}

```

```
function findNeedle(haystack) {

  return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "  + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf("needle")

}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```

function findNeedle(haystack){
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " +haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
    return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
 return "found the needle at position " + haystack.indexOf("needle");
 }
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {

  return "found the needle at position " + haystack.indexOf("needle");

}
```

```
function findNeedle(haystack) {
    return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+ haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf("needle");
}

```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
  // your code here
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + (haystack.indexOf('needle'));
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
    return 'found the needle at position ' + haystack.indexOf('needle');

}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
    return "found the needle at position " + haystack.indexOf('needle');

}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
   return "found the needle at position " +haystack.indexOf('needle');
  }
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
 return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position "+haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
return "found the needle at position " + haystack.indexOf("needle");

}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle")
}


```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
  // your code here
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
   return "found the needle at position " + haystack.indexOf('needle');
 }
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
return 'found the needle at position ' + haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {

  // your code here
  return 'found the needle at position '+ haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  // your code here
  return("found the needle at position " + haystack.indexOf("needle"));
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position "+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {

return "found the needle at position " + haystack.indexOf("needle")


}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
 return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle");

}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
    return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  // your code here
  return ('found the needle at position ') + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  return'found the needle at position ' +haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf("needle");

}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  // your code here
  return 'found the needle at position ' + haystack.indexOf('needle')
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position '+haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {

  return "found the needle at position " + haystack.indexOf("needle")

}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {


// your code here

return 'found the needle at position ' + haystack.indexOf('needle');

}

```

```
function findNeedle(haystack) {
  // your code here

  return('found the needle at position ' + haystack.indexOf('needle'));
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position " + haystack.indexOf("needle");
}
```

```
function findNeedle(haystack) {
  // your code here
  return "found the needle at position "+haystack.indexOf("needle")
}
```

```
function findNeedle(haystack) {
  return "found the needle at position " + haystack.indexOf("needle");
  }
```

```
function findNeedle(haystack) {
  return 'found the needle at position ' + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
return "found the needle at position "+ haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
 return "found the needle at position " + haystack.indexOf('needle');
}
```

```
function findNeedle(haystack) {
    return 'found the needle at position ' + haystack.indexOf('needle');
}
```
