# find-maximum-and-minimum-values-of-a-list
// https://www.codewars.com/kata/find-maximum-and-minimum-values-of-a-list/solutions/javascript/me/best_practice


```
var min = function(list){
    return Math.min.apply(null,list);
}

var max = function(list){
    return Math.max.apply(null,list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){return Math.min.apply(null, list)}
var max = function(list){return Math.max.apply(null, list)}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

  return Math.max.apply(null,list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);;
}
```

```
var min = function(list){
  return Math.min.apply(null,list);
}

var max = function(list){
  return Math.max.apply(null,list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
  return Math.min.apply(null,list);
}

var max = function(list){
  return Math.max.apply(null,list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
  return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
  return Math.min.apply(null, list);
}

var max = function(list){
  return Math.max.apply(null, list);
}
```

```
var min = function(list){
  return Math.min.apply(null, list);
}

var max = function(list){
  return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);

}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}

```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

  return (Math.min.apply(null, list));
}

var max = function(list){

  return (Math.max.apply(null, list));
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
   return Math.min.apply(null,list)
    ;
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){
   return Math.min.apply(null,list);

}

var max = function(list){
   return Math.max.apply(null,list);

}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);

}

var max = function(list){

    return Math.max.apply(null, list);

}
```

```
var min = function(list){

    return Math.min.apply(null, list);;
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list)
}

var max = function(list){
    return Math.max.apply(null, list)
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
   return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);

}
```

```
var min = function(list){
  return Math.min.apply(null, list);
}

var max = function(list){
  return Math.max.apply(null, list);
}
```

```
var min = function(list){
   return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
  return Math.max.apply(null, list)
}
```

```
var min = function(list){

    return  Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list)
}

var max = function(list){
    return Math.max.apply(null, list)
}
```

```
var min = function(list) {
    return Math.min.apply(null, list);
}

var max = function(list) {
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

     return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
  return Math.min.apply(null,list);
}

var max = function(list){
    return Math.max.apply(null,list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);;
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){


    return Math.min.apply(null, list)

}

var max = function(list){

    return Math.max.apply(null, list)
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
  return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){
  return Math.min.apply(null, list);
}

var max = function(list){
  return Math.max.apply(null, list);

}
```

```
var min = function(list){
  return Math.min.apply(null, list);
  }
  var max = function(list){
  return Math.max.apply(null, list);
  }
```

```
var min = function(list){
  return Math.min.apply(null, list);
}

var max = function(list){
  return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
    return list[0];
}

var max = function(list){
    return Math.max.apply(null, list);
    return list[0];
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
return Math.min.apply(null,list);
}

var max = function(list){
    return Math.max.apply(null,list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return  Math.max.apply(null, list);
}
```

```
var min = function(list){
  return Math.min.apply(null, list);
}

var max = function(list){
  return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
  return Math.min.apply(null, list);
}

var max = function(list){
  return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){return Math.min.apply(null, list)}
var max = function(list){return Math.max.apply(null, list)}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
  return Math.min.apply(null, list);
}

var max = function(list){
  return Math.max.apply(null, list);
}
```

```
var min = function(list){

     return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
  return Math.min.apply(null, list)
}

var max = function(list){
  return Math.max.apply(null, list)
}
```

```
var min = function(list){
    return Math.min.apply(null, list)

}

var max = function(list){

    return Math.max.apply(null, list)
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
    //return list[0];
}

var max = function(list){
   return Math.max.apply(null, list);

}
```

```
var min = function(list){

    //return list[0];
    return Math.min.apply(null, list);
}

var max = function(list){

    //return list[0];
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
  return Math.min.apply(null, list);
}

var max = function(list){
  return Math.max.apply(null, list);
}
```

```
var min = function(list){
      return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null,list)
}

var max = function(list){
    return Math.max.apply(null,list)
}
```

```
var min = function(list) {
    return Math.min.apply(null, list);
}

var max = function(list) {
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
  return Math.min.apply(null,list);
}

var max = function(list){
    return Math.max.apply(null,list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

     return Math.max.apply(null,list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

   return Math.min.apply(null, list);
}

var max = function(list){

   return Math.max.apply(null, list);

}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
 return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

   return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
  return Math.min.apply(null, list);
}

var max = function(list){
  return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
    return list[0];
}

var max = function(list){
    return Math.max.apply(null, list);
    return list[0];
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list) ;
}

var max = function(list){

    return Math.max.apply(null, list) ;
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

     return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
    //apply() is used because list is an array
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list) {
    return Math.min.apply(null, list);
}

var max = function(list) {
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
  return Math.min.apply(null, list);
}
var max = function(list){
  return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
     return Math.max.apply(null, list);
}
```

```
var min = function(list){
  return Math.min.apply(null, list);
}

var max = function(list){
  return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);

}
```

```
var min = function(list){
  return Math.min.apply(null, list)
}

var max = function(list){
  return Math.max.apply(null, list)
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
};

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null,list);
}

var max = function(list){
    return Math.max.apply(null,list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
  return Math.min.apply(null,list);
}

var max = function(list){
  return Math.max.apply(null,list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list)
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
     return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list)

}

var max = function(list){

    return Math.max.apply(null, list)
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list) {
  return Math.min.apply(null, list);
}

var max = function(list) {
  return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);

}

var max = function(list){
   return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
   return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);

}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
     return Math.min.apply(null, list);
}

var max = function(list){
     return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);;
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

   return Math.max.apply(null,list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null,list);
}

```

```
var min = function(list){
  return Math.min.apply(null, list);
}

var max = function(list){
  return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);;
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list)
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return  Math.min.apply(null, list);
}

var max = function(list){

    return  Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list)
}

var max = function(list){
    return Math.max.apply(null, list)
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
  return Math.min.apply(null, list);

}

var max = function(list){
 return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list)
}

var max = function(list){

    return Math.max.apply(null, list)
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list)
}

var max = function(list){

    return Math.max.apply(null,list)
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list)
}

var max = function(list){
    return Math.max.apply(null, list)
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){
  return Math.min.apply(null, list);
}

var max = function(list){
  return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
  return Math.min.apply(null,list);
}

var max = function(list){
  return Math.max.apply(null,list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

   return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

          return Math.min.apply(null,list)
}

var max = function(list){

         return Math.max.apply(null, list)
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
  return Math.min.apply(null, list);
}

var max = function(list){
 return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
     return Math.max.apply(null, list);
}

```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
  var min = function(list){

        return Math.min.apply(null,list);

    }

    var max = function(list){
        return Math.max.apply(null,list);
    }
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){
    return  Math.min.apply(null, list);
}

var max = function(list){
     return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
  return Math.min.apply(null, list);
}

var max = function(list){
  return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list)
}

var max = function(list){

   return Math.max.apply(null, list)
}
```

```
var min = function(list){

    return  Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){
  return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

return Math.min.apply(null, list);

}

var max = function(list){

  return Math.max.apply(null, list);
}

```

```
var min = function(list){
    return Math.min.apply(null,list);
}

var max = function(list){
    return Math.max.apply(null,list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
    return list[0];
}

var max = function(list){
    return Math.max.apply(null, list);
    return list[0];
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){
  return Math.min.apply(null, list);
}

var max = function(list){
  return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null ,list);
}

var max = function(list){
    return Math.max.apply(null ,list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null,list);
}

var max = function(list){
    return Math.max.apply(null,list);
}
```

```
var min = function(list){
  return Math.min.apply(null, list)
}

var max = function(list){
  return Math.max.apply(null, list)
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){
  return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){

return Math.min.apply(null,list);
}

var max = function(list){
return Math.max.apply(null,list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```

var min = function(list){
    return Math.min.apply(null,list);
}

var max = function(list){
    return Math.max.apply(null,list);
}

```

```
var min = function(list){
    //st.sort(function(a, b) {return a > b ? 1 : -1} )
    //return list[0];
    return Math.min.apply(null,list)
}

var max = function(list){
    //st.sort(function(a, b) {return a > b ? 1 : -1} )
    //return list[list.length-1];
    return Math.max.apply(null,list)
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){

    return Math.max.apply (null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null,list)
}

var max = function(list){

    return Math.max.apply(null,list)
}
```

```
var min = function(list){
 return Math.min.apply(null, list)
}

var max = function(list){
  return  Math.max.apply(null, list)
}
```

```
var min = function(list){
  return Math.min.apply(null,list)
}

var max = function(list){
    return Math.max.apply(null,list)
}
```

```
var min = function(list){
return Math.min.apply(null, list);

}

var max = function(list){
return Math.max.apply(null, list);

}
```

```
var min = function(list){

    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){

    return Math.min.apply(null,list);
}

var max = function(list){

    return Math.max.apply(null,list);
}
```

```
var min = function(list){
     return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
    return Math.min.apply(null, list)
}
var max = function(list){
    return Math.max.apply(null, list)
}
```

```
var min = function(list){
    return Math.min.apply(null, list);
}

var max = function(list){
    return Math.max.apply(null, list);
}
```

```
var min = function(list){
  return Math.min.apply(null, list);
}

var max = function(list){
  return Math.max.apply(null, list);
}
```
